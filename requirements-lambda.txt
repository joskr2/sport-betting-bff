# =======================================================================
# 🚀 AWS Lambda Requirements - Minimal Version
# =======================================================================
# Only dependencies actually used by the project

# === Core FastAPI Stack ===
fastapi==0.68.0
starlette==0.14.2

# === Data Validation ===
pydantic==1.10.12

# === HTTP Client ===
httpx==0.24.1

# === Lambda Adapter ===
mangum==0.17.0

# === Utilities ===
python-dotenv==1.0.0
typing-extensions==4.6.0

# === Caching (used by backend_service.py) ===
cachetools==5.3.3

# === Async Support (required by httpx) ===
anyio>=3.0,<5.0
sniffio>=1.1

# === HTTP Dependencies (required by httpx) ===
httpcore>=0.15.0,<0.18.0
h11>=0.13,<0.15
certifi
idna